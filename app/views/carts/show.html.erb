<h2>Your Cart</h2>

<ul>
  <% @cart.line_items.each do |product| %>
      <li><%= product.quantity %> &times; <%= product.item.title %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_cart_path(@cart) %> |
<%= link_to 'Back', carts_path %>
